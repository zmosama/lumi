version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: lumi_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: lumi
      POSTGRES_PASSWORD: lumi_password
      POSTGRES_DB: lumi_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lumi -d lumi_db"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
